{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"No frontend build configured. This project uses server-rendered Blade + Bootstrap.\"",
    "dev": "echo \"Dev script disabled. Use php artisan serve for Laravel dev server.\""
  },
  "devDependencies": {},
  "dependencies": {}
}
